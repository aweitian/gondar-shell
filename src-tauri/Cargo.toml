[package]
name = "tauri-app"
version = "0.0.0"
edition = "2021"

[dependencies]
tauri = { version = "2.11.0", features = ["default"] }

[build-dependencies]
tauri-build = { version = "2.6.2", features = [] }
